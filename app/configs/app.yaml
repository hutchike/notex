# Application settings in YAML format (see http://www.yaml.org/ for details)

# For a full list of timezones, see http://www.php.net/manual/en/timezones.php

ini:
    date.timezone: America/Mexico_City
    log_errors: true
    track_errors: true
    display_errors: true
    session.cookie_domain: .noted.cc

settings:
    testing_enabled: true
    logging_enabled: true
    benchmarking_on: true
    exception_redirect: NULL
    default_log_level: info
    params_trim_whitespace: true
    params_format_as_html: false
    params_strip_slashes: true
    supported_languages: [en]
    default_language: en
    cookie_domain: .noted.cc
    cache_expiry_secs: 3600

testing:
    contact_email: support@notex.com
    webmaster_email: webmaster@notex.com
    app_name: noteX
    app_test_controller_list: [User]
    app_test_view_path_list: [/, user/login, user/logout]

database:
    connector: SQLite3
    hostname: localhost
    username: root
    password: 
    database_live: app/data/notex_live.db
    database_test: app/data/notex_test.db

content:
    file_url_prefix: /
    view_url_prefix: /
    default_content_type: html
    default_controller: App
    default_folder: default
    default_file: index
    default_extension: .php
    file_not_found: not_found

# End of app.yaml
